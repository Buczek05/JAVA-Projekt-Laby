package com.example.budget.entity;

/**
 * Enum representing user roles in the system.
 * ADMIN: Can edit and delete objects
 * USER: Can add and view objects
 */
public enum Role {
    ADMIN,
    USER
}